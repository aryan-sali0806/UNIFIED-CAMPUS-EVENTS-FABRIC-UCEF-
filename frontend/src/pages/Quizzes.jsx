import PagePlaceholder from "../components/PagePlaceholder";

const Quizzes = () => {
  return (
    <PagePlaceholder
      title="Quizzes ❓"
      subtitle="Quiz events, attempts, and results will appear here."
    />
  );
};

export default Quizzes;
